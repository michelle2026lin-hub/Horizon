# Melody Script Map（代码级全景图）
> 版本: Code-Level v3.0
> 用途: AI 筛选系统唯一真理源
> 更新: 2026-07（模块16废弃并入17；17.1/17.3细化；新增19客户矩阵/20水印/21分发/22对账）

---

## 全局常量（AI 必读 · 不可修改）

| 常量 | 值 | 所在脚本 |
|---|---|---|
| **P_HASH_THRESHOLD** | `8`（汉明距离） | `final_v14.py` |
| **MAX_HISTORY** | `20`（最近消息条数） | `wa_reply_bot.js` |
| **DAILY_LIMIT_WA** | `200`（每日发送上限） | `whatsapp_auto.py` |
| **SLEEP_RANGE_WA** | `15-30s`（随机等待，防封号） | `whatsapp_auto.py` |
| **API_RETRY** | `3`（失败重试次数） | 所有API调用 |
| **BACKOFF** | `1s, 2s, 4s`（指数退避） | 所有API调用 |
| **RXID_FORMAT** | `RXID__{record_id}__{original_name}` | 所有文件操作 |
| **COORD_FILE** | `wa_coords.json` | `whatsapp_auto.py`, `calibrate_wa.py` |
| **PROXY_PORT** | `18899` | `generate_outreach_list.py` |
| **PAGE_SIZE** | `100`（分页大小） | 所有飞书API调用 |
| **TOKEN_REFRESH** | `5min`（距过期刷新） | `generate_outreach_list.py` |
| **WAIT_DOWNLOAD** | `60s`（确认下载完成） | `watcher.py` |
| **CONCURRENCY** | `5`（账号并发数） | `watcher.py` |
| **MIN_CHAPTERS_1000** | `20`个章节（原分镜数再叠一轮，原创实拍章节≥15） | autocut.video（19.2） |
| **MIN_MATERIALS_1000** | 每章节：原创实拍≥20 / 网络拆片≥50 | autocut.video（19.2） |
| **MIN_CHAPTERS_100** | `20`个章节（同上） | autocut.video（19.2） |
| **MIN_MATERIALS_100** | 每章节：原创实拍≥10 / 网络拆片≥10 | autocut.video（19.2） |
| **VOICEOVER_CLIP_LIMIT** | 整段配音成片 ≤ `5`条（需逐章节配音才能规模化） | autocut.video（19.2/19.3） |

---

## 二创提示词规则集（AI 宪法 · 非代码但优先级最高）

| Rule ID | 规则内容 | 🔍可被替代 | 📈可被升级 | ⚠️红线 |
|---|---|---|---|---|
| R.1 | **Logo 处理**: 必须使用 `hottai_logo.png`，禁止重绘/变形/变色，仅等比缩放 | Logo 检测算法 | 矢量 Logo 替换 | **严禁修改 Logo** |
| R.2 | **色系调整**: 装饰色 → `#CC0000`，不改产品本体颜色，保留黑白灰文字 | 颜色替换算法 | 智能着色 | **严禁改变产品颜色** |
| R.3 | **黑白名单**: 白名单(铰链/滑轨)，黑名单(拉手/锁具/螺丝)，混合图只消黑名单 | 物体检测 | 语义分割 | **严禁误杀白名单** |
| R.4 | **原位消除**: 水印/证书/QR/竞对 Logo/印度国旗，无缝填充背景 | Inpainting | 高清修复 | **消除需自然** |
| R.5 | **文字替换**: 公司名/地址/电话/WhatsApp/邮箱，印度地名→揭阳，MADE IN INDIA→CHINA | OCR+翻译 | 多语言生成 | **严禁新增原图无信息** |
| R.6 | **Logo 替换**: 所有竞对 Logo → HOTTAI Logo，包装袋/盒同步 | Logo 检测+替换 | 3D Logo 贴合 | **严禁遗漏 Logo** |
| R.7 | **白名单保护**: 不改产品本体/构图/光影/背景/卖点文字，输出尺寸一致 | 一致性约束 | 风格迁移控制 | **严禁改变产品构图** |

---

## 1. final_v14.py（AI 识别与入库引擎）

| ID | 代码级原子子步骤 | 🔍可被替代 | 📈可被升级 | ⚠️红线 (AI严禁触碰) |
|---|---|---|---|---|
| 1.1 | **遍历 Input 目录**: `os.walk('~/Desktop/input')`，识别父目录为账号名，区分 INS/FB/MAGAZINE 源 | Watchdog 文件系统监听 | 云盘直读 (GDrive/OneDrive) | 必须处理"待二创"目录跳过逻辑 |
| 1.2 | **JSON 读取**: 读取同目录 `文案+标签` 下 JSON，提取 Caption | JSON 解析库 | 流式读取 | 必须处理 JSON Decode Error |
| 1.3 | **正则提取手机号**: `r'(\d{10})'` 及 `r'91\d{10}'`，去重 | libphonenumber | AI 语义提取 | 不得误判 WhatsApp 号 |
| 1.4 | **黑名单强杀**: 匹配关键词(螺丝/拉手/expo)，命中即终止 | Trie 关键词树 | NLP 语义过滤 | 不得放行黑名单关键词 |
| 1.5 | **灰度化与缩放**: `cv2.cvtColor(BGR2GRAY)` → `cv2.resize(8x8)` | Pillow-SIMD | GPU 加速 (CUDA) | **必须 8x8 尺寸** |
| 1.6 | **DCT 变换**: `cv2.dct()`，生成 64 位哈希 | 哈希算法 | 局部敏感哈希 (LSH) | 算法变更需验证误杀率 |
| 1.7 | **汉明距离计算**: 比对缓存库，**[Red Line] 阈值 ≤ 8** | 向量相似度 | 动态阈值 | **严禁修改阈值 8** |
| 1.8 | **飞书黑名单查询**: `GET /bitable/v1/tables/{table_id}/records` | Redis (毫秒级) | 向量数据库 | 不得跳过查询 |
| 1.9 | **RGBA转RGB**: `Image.convert('RGB')`，白底填充 | 图像预处理库 | WebP/HEIC 原生支持 | 不得改变主体色彩 |
| 1.10 | **图片压缩**: 最长边≤1024，Quality=85% | 压缩算法 | 自适应压缩 | 不得损失关键细节 |
| 1.11 | **21模型轮询**: 遍历16账号×21模型，FreeTier耗尽切下一模型 | 模型路由框架 | 多模态大模型 (GPT-4o) | **必须保留轮询逻辑** |
| 1.12 | **VL 调用**: `POST /services/vision_diagnose`，提取标签/公司/电话/网址 | 任意 VL API | 本地 VL 模型 (LLaVA) | 不得减少返回字段 |
| 1.13 | **飞书客户查重**: 按公司名模糊匹配 `customer_table` | ES 模糊查询 | 向量检索 | 不得重复创建客户 |
| 1.14 | **飞书字段写入**: `PATCH /bitable/...`，写入标签/文案/关联客户 | ORM 框架 | 批量写入 API | 必须保留原字段追加逻辑 |
| 1.15 | **RXID 重命名**: `shutil.move(old, new)`，格式 `RXID__{record_id}__` | 文件操作 | 云存储 Key 映射 | **严禁破坏 RXID 命名** |
| 1.16 | **本地清理**: `os.remove()` 源图及 JSON | 文件清理 | 回收站机制 | 黑名单图片不得入素材库 |
| 1.17 | **异常兜底**: Try-Except，保留异常文件，抛异常给 Watcher | 日志系统 | 死信队列 | 不得静默失败 |

---

## 2. whatsapp_auto.py（批量开发信执行器）

| ID | 代码级原子子步骤 | 🔍可被替代 | 📈可被升级 | ⚠️红线 (AI严禁触碰) |
|---|---|---|---|---|
| 2.1 | **加载坐标**: `json.load('wa_coords.json')`，读取 `send_btn`, `close_btn` 等 | 无坐标自动化 | Browser-Use / TARS | **依赖物理屏幕** |
| 2.2 | **JS 扫描按钮**: `document.querySelectorAll('.btn-wa:not(.used)')` | DOM 选择器 | MutationObserver | 必须过滤已 used 按钮 |
| 2.3 | **坐标换算**: `element.getBoundingClientRect()` + 滚动偏移 | 视口计算 | 相对坐标 | 多显示器环境下易失效 |
| 2.4 | **JS Click**: `element.click()`，触发 WhatsApp Web | 浏览器自动化 | Playwright / Puppeteer | 不得双击 |
| 2.5 | **截图判定**: `pyautogui.screenshot(region=send_btn_area)` | 屏幕捕获 | DOM 元素截图 | 依赖屏幕分辨率 |
| 2.6 | **像素统计**: `np.count_nonzero(img == GREEN)`，**[Red Line] >20 判定成功** | 颜色识别算法 | CV 模板匹配 | **严禁修改绿色判定阈值** |
| 2.7 | **弹窗检测**: 截图统计深色像素，判定"无 WhatsApp 账号" | 图像分类 | UI 元素识别 | 误判导致漏发 |
| 2.8 | **PyAutoGUI 点击**: `click(coords['wa_send_btn'])` | 系统级自动化 | Chrome DevTools Protocol | 屏幕遮挡即失败 |
| 2.9 | **随机等待**: `time.sleep(random.uniform(15, 30))` | 固定等待 | 智能等待 (页面加载) | **严禁缩短等待时间** |
| 2.10 | **HTML 状态回写**: `element.classList.add('used')`，`fs.writeFileSync()` | 本地文件锁 | 飞书状态实时同步 | **刷新页面不得丢失状态** |
| 2.11 | **每日限额**: 计数器累加，**[Red Line] 达到 200 停止** | 限流算法 | 分布式限流 | **严禁突破 200 条上限** |
| 2.12 | **熔断机制**: 连续5次状态不明 → `time.sleep(30)` | 熔断机制 | 自适应熔断 | 防止账号风控 |

---

## 3. wa_reply_bot.js（客服 AI Agent）

| ID | 代码级原子子步骤 | 🔍可被替代 | 📈可被升级 | ⚠️红线 (AI严禁触碰) |
|---|---|---|---|---|
| 3.1 | **WS 连接飞书**: `new WebSocket('wss://...')`，监听指令 | Webhook | Kafka / RabbitMQ | 断线需重连 |
| 3.2 | **过滤己方消息**: `message.fromMe === true`，跳过 | 消息过滤 | 会话隔离 | 不得回复自己 |
| 3.3 | **提取历史**: 拉取 **[Red Line] 最近 20 条**消息 | 历史长度 | 向量检索 (语义记忆) | **严禁缩减历史条数** |
| 3.4 | **客户建档**: 新号码 → `POST /bitable/...` 创建线索 | CRM API | 客户 360 视图 / Agent记忆系统 | 不得丢失客户来源 |
| 3.5 | **Prompt 注入**: 注入定价/市场/画像/竞对话术/去机器味/WhatsApp三步筛选/Google Drive查图 | Prompt 工程 / RAG | 知识库增强 | 不得遗漏竞对话术 |
| 3.6 | **Groq API 调用**: `fetch('https://api.groq.com/...', {model: 'llama-3.3-70b'})` | LLM API | Claude API / GPT-4o | 需处理 429 限流 |
| 3.7 | **飞书审核推送**: 格式化输出，等待 `#1/#2/#3` 指令 | 人机回环 | 高置信度自动通过 | 不得绕过人工审核 |
| 3.8 | **指令分发**: `'1'` → 发送；`'2'` → 跳过；`'3'` → 重生成 | 指令解析 | 自然语言指令 | 指令逻辑不得混淆 |
| 3.9 | **飞书回写**: 更新客户画像/压价话术/下一步行动 | 数据回流 | 自动标签更新 | 画像数据不得丢失 |

---

## 4. competitor_intel_bot.js（竞对情报 Agent）

| ID | 代码级原子子步骤 | 🔍可被替代 | 📈可被升级 | ⚠️红线 (AI严禁触碰) |
|---|---|---|---|---|
| 4.1 | **预加载话术**: 每1小时缓存 `leads` 表压价话术 | 缓存策略 | 实时订阅 | 话术过期需刷新 |
| 4.2 | **分页拉取情报表**: `GET /bitable/...?page_size=100` | 分页逻辑 | 游标分页 / Firecrawl | 不得遗漏厂家 |
| 4.3 | **号码映射**: 构建 `phone → {recordId, intel}` Map | 数据索引 | 内存数据库 | 映射关系不得错乱 |
| 4.4 | **6步谈判 Prompt**: 注入历史/情报/模板，生成下一条消息 | 谈判策略库 | 强化学习 (RL) | 不得跳过谈判步骤 |
| 4.5 | **Groq 退避重试**: `429` → `setTimeout(fn, 2000)` | 重试机制 | 指数退避算法 | 不得无限重试 |
| 4.6 | **新厂建记录**: 未知号码 → `POST /bitable/...` 自动新建 | 自动录入 | 实体解析 | 不得拒绝录入新厂 |
| 4.7 | **实时监听**: `whatsapp.on('message', handler)` | 事件驱动 | 流式处理 | 消息丢失需告警 |

---

## 5. watcher.py（主控与熔断）

| ID | 代码级原子子步骤 | 🔍可被替代 | 📈可被升级 | ⚠️红线 (AI严禁触碰) |
|---|---|---|---|---|
| 5.1 | **监控目录**: `watchdog.observers.Observer('~/Desktop/input')` | 轮询 | Inotify / FSEvents | 必须排除系统文件 |
| 5.2 | **等待下载**: `time.sleep(60)`，确认文件完整 | 文件锁 | 校验文件 Hash | 不得处理未完成文件 |
| 5.3 | **分组并发**: 按账号分组，**[Red Line] 5 账号并发** | 线程池 | 协程 (Asyncio) | **严禁超限并发** |
| 5.4 | **顺序调用**: 组内顺序执行 `final_v14.py` | 任务队列 | 优先级队列 | 不得乱序 |
| 5.5 | **403/超时切换**: 捕获异常，切下一模型/账号 | 故障转移 | 负载均衡 | 必须保留切换逻辑 |
| 5.6 | **pHash 去重**: 调用 `sync_dedup_cache.py` 逻辑 | 去重算法 | 布隆过滤器 | 不得重复处理图片 |
| 5.7 | **报警停止**: 所有账号耗尽 → `print('ALARM')` 并退出 | 告警系统 | 飞书/邮件告警 | 不得静默失败 |

---

## 6. generate_outreach_list.py（客户列表生成）

| ID | 代码级原子子步骤 | 🔍可被替代 | 📈可被升级 | ⚠️红线 (AI严禁触碰) |
|---|---|---|---|---|
| 6.1 | **启动代理**: `HTTPServer(('127.0.0.1', 18899), Handler)` | 反向代理 | 飞书 Automation | **端口占用即失败** |
| 6.2 | **Token 刷新**: 距过期5分钟自动刷新 Tenant Token | OAuth2 | 长效 Token | Token 失效需告警 |
| 6.3 | **电话解析**: 正则 `r'(\d{10})'`，格式化 `+91/+86` | 解析库 | libphonenumber / Scrapling | 不得误解析邮箱 |
| 6.4 | **全局去重**: `Set()` 存储已见号码，按数量排序 | 数据结构 | Bloom Filter | 不得重复添加客户 |
| 6.5 | **HTML 注入**: 内联 CSS/JS，绑定 `handleWA/toggleCopy` | 前端框架 | React / Vue | 不得破坏按钮逻辑 |
| 6.6 | **预置灰色**: 已开发号码 `classList.add('used')` | 状态同步 | 飞书实时状态 | 状态需持久化 |
| 6.7 | **指数退避**: `time.sleep(1)` → `2` → 放弃 | 重试策略 | 批量写入 API | 不得丢弃失败记录 |

---

## 7. generate_pi_pl.py（合同生成）

| ID | 代码级原子子步骤 | 🔍可被替代 | 📈可被升级 | ⚠️红线 (AI严禁触碰) |
|---|---|---|---|---|
| 7.1 | **PI 号输入**: `input('Enter PI No: ')`，非空校验 | CLI | 飞书按钮触发 | 不得接受空 PI 号 |
| 7.2 | **分页拉取**: `page_size=100`，精确匹配 PI 号 | 查询逻辑 | 索引优化 | 不得拉取无关数据 |
| 7.3 | **Sheet 构建**: `wb.create_sheet('PI')`，设置列宽/行高 | Excel 库 | 模板引擎 | **必须适配 A4 打印** |
| 7.4 | **双层表头**: 合并单元格，写入货描/单价/数量等 | 复杂表格 | 动态表头 | 表头结构不得变更 |
| 7.5 | **自动计算**: 箱数/CBM/净重/毛重/总金额 | 公式计算 | 财务引擎 / Office文档AI工具 | 计算结果必须精确 |
| 7.6 | **文件保存**: `wb.save(f'PI_PL_{pi_no}.xlsx')` | 文件操作 | PDF 直出 / OfficeCLI | 不得覆盖已有文件 |

---

## 8. download_to_pending.py（二创素材下载）

| ID | 代码级原子子步骤 | 🔍可被替代 | 📈可被升级 | ⚠️红线 (AI严禁触碰) |
|---|---|---|---|---|
| 8.1 | **参数解析**: `argparse('--all', '--tag', '--customer')` | CLI | Web UI | 参数冲突需校验 |
| 8.2 | **分页拉取**: `page_size=100`，`has_more` 翻页 | 分页逻辑 | 游标分页 | 不得遗漏记录 |
| 8.3 | **字段筛选**: 必须有图片，默认二创为空 | 过滤逻辑 | 增量同步 | 不得下载已二创 |
| 8.4 | **RXID 命名**: `f'{record_id}_{filename}'` | 命名规则 | 哈希命名 | **严禁破坏 RXID** |
| 8.5 | **存在检查**: `os.path.exists()`，跳过已存在 | 去重逻辑 | ETag 校验 | 不得重复下载 |
| 8.6 | **API 下载**: `GET /drive/v1/medias/{token}/download` | 下载逻辑 | 断点续传 | 下载失败需重试 |
| 8.7 | **限流等待**: `time.sleep(0.2)` | 限速 | 令牌桶 | **严禁取消等待** |

---

## 8.5 图片二创执行（listenhub.ai）

**说明**：本节承接 module 8 下载素材、衔接 module 9 回传素材，此前从未单独建模块，导致工具筛选时无法精确匹配"二创执行"这一环。

| ID | 原子子步骤 | 🔍现役/候选实现 | ⚠️红线 |
|---|---|---|---|
| 8.5.1 | **拖入待二创图片**至 listenhub.ai | listenhub.ai（现役） | 底层模型 gpt-image-2lite |
| 8.5.2 | **应用二创提示词规则**（引用 `二创提示词.txt` R.1-R.7） | 二创提示词（宪法级） | 严禁触碰 R.1-R.7 任一红线 |
| 8.5.3 | **生成结果下载**至【二创完成】文件夹 | listenhub.ai | 每账号100积分≈20张，用完换号 |
| 8.5.4 | **文件名混乱问题**：需手动改名匹配原始文件名 | 人工（未解决痛点） | **此步骤是当前效率瓶颈，优先筛选可替代目标** |

---

## 9. upload_recreated.py（二创回传）

| ID | 代码级原子子步骤 | 🔍可被替代 | 📈可被升级 | ⚠️红线 (AI严禁触碰) |
|---|---|---|---|---|
| 9.1 | **扫描目录**: `glob.glob('*.jpg/png')` | 文件遍历 | Watchdog | 必须处理子目录 |
| 9.2 | **RXID 提取**: `re.match(r'RXID__(.+)__(.+)')` | 正则提取 | 结构化解析 | **RXID 解析失败即终止** |
| 9.3 | **飞书上传**: `POST /drive/v1/files/upload` | 上传逻辑 | 分片上传 / Rephael AI | 上传失败需重试 |
| 9.4 | **追加字段**: `PATCH /bitable/...`，追加到多附件字段 | 字段更新 | 数组追加 API | **严禁覆盖原有附件** |
| 9.5 | **移动文件**: `shutil.move('二创完成', '已回传')` | 文件归档 | 云存储生命周期 | 回传成功才能移动 |

---

## 10. sync_history_to_feishu.js（历史回传）

| ID | 代码级原子子步骤 | 🔍可被替代 | 📈可被升级 | ⚠️红线 (AI严禁触碰) |
|---|---|---|---|---|
| 10.1 | **连接 WA**: `whatsapp.connect()`，LocalAuth 持久化 | 连接方式 | WhatsApp Business API | 需处理重连 |
| 10.2 | **拉取情报**: `GET /bitable/...`，获取所有厂家号码 | 数据拉取 | 增量拉取 | 不得遗漏厂家 |
| 10.3 | **匹配私聊**: 遍历 Chats，匹配号码 | 匹配逻辑 | 模糊匹配 | 匹配失败需记录 |
| 10.4 | **拉取历史**: 获取 **[Red Line] 最近 200 条**消息 | 历史深度 | 全量历史 | **严禁缩减历史条数** |
| 10.5 | **Groq 分析**: 提取产品线/报价/话术/活跃度 | 分析模型 | 结构化提取模型 | 不得丢失关键信息 |
| 10.6 | **断点续传**: 记录已处理 ID，跳过已处理 | 进度保存 | 状态机 | 不得重复处理 |

---

## 11. sync_dedup_cache.py（指纹同步）

| ID | 代码级原子子步骤 | 🔍可被替代 | 📈可被升级 | ⚠️红线 (AI严禁触碰) |
|---|---|---|---|---|
| 11.1 | **拉取素材库**: `GET /bitable/...`，提取 pHash | 数据提取 | 流式提取 | 不得遗漏 pHash |
| 11.2 | **拉取黑名单**: 提取黑名单指纹 | 数据提取 | 规则引擎 | 不得遗漏黑名单 |
| 11.3 | **合并缓存**: `dict.update()`，去重合并 | 合并逻辑 | 版本控制 | 不得丢失旧指纹 |
| 11.4 | **本地保存**: `json.dump('dedup_cache.json')` | 持久化 | 数据库 (SQLite) | 文件损坏需恢复 |

---

## 12. split_data.py（IG 数据分拣）

| ID | 代码级原子子步骤 | 🔍可被替代 | 📈可被升级 | ⚠️红线 (AI严禁触碰) |
|---|---|---|---|---|
| 12.1 | **读取 JSON**: `json.load('data_hardware_time_magzine.json')` | 文件读取 | 流式 JSON | 文件损坏需处理 |
| 12.2 | **Shortcode 拆分**: 按 `shortcode` 分组数据 | 分组逻辑 | 哈希分片 | 不得丢失数据 |
| 12.3 | **图片分离**: 图片 → `图片库/账号ID/` | 文件操作 | 云存储 | 目录结构不得变更 |
| 12.4 | **文案分离**: 文案+标签 → `文案+标签/账号ID/` | 文件操作 | 数据库 | 关联关系不得丢失 |
| 12.5 | **杂志分类**: 按杂志/账号建子文件夹 | 分类逻辑 | 元数据标签 | 分类规则需一致 |

---

## 13. wechat_auto.py（微信加好友）

| ID | 代码级原子子步骤 | 🔍可被替代 | 📈可被升级 | ⚠️红线 (AI严禁触碰) |
|---|---|---|---|---|
| 13.1 | **拉取线索**: `GET /bitable/...`，过滤 `已回复/待检测` | 数据拉取 | 实时订阅 | 不得遗漏线索 |
| 13.2 | **激活窗口**: `pyautogui.hotkey('cmd', 'tab')`，切换微信 | 窗口管理 | AppleScript / Win32 | 依赖前台窗口 |
| 13.3 | **点击加号**: `click(coords['add_button'])` | 坐标点击 | UI 自动化 / AI Compartner | 坐标失效即失败 |
| 13.4 | **输入电话**: `pyautogui.typewrite(phone)` | 键盘输入 | 剪贴板粘贴 | 输入法需英文 |
| 13.5 | **OCR 识别**: `pytesseract.image_to_string(screenshot)` | OCR 引擎 | 深度学习 OCR / PaddleOCR | 识别失败需重试 |
| 13.6 | **随机等待**: `time.sleep(random.uniform(...))` | 防封控 | 拟人化操作 | **严禁固定等待时间** |
| 13.7 | **结果回写**: `PATCH /bitable/...`，更新状态 | 数据回写 | 事务回滚 | 状态需准确 |

---

## 14. calibrate_wa.py（坐标校准）

| ID | 代码级原子子步骤 | 🔍可被替代 | 📈可被升级 | ⚠️红线 (AI严禁触碰) |
|---|---|---|---|---|
| 14.1 | **发送按钮**: 人工点击 WA 发送箭头，记录坐标 | 坐标记录 | 自动探测 UI | **依赖人工操作** |
| 14.2 | **窗口关闭**: 记录左上角红色 ✕ | 坐标记录 | 自动探测 UI | **依赖人工操作** |
| 14.3 | **Tab 关闭**: 记录标签页 ✕ | 坐标记录 | 自动探测 UI | **依赖人工操作** |
| 14.4 | **弹窗确定**: 记录"无 WA"弹窗确定按钮 | 坐标记录 | 自动探测 UI | **依赖人工操作** |
| 14.5 | **导出 JSON**: `json.dump(wa_coords.json)` | 配置导出 | 云端同步 | 文件需可读写 |

---

## 15. 脚本控制面板.html（PM2 管理面板）

| ID | 代码级原子子步骤 | 🔍可被替代 | 📈可被升级 | ⚠️红线 (AI严禁触碰) |
|---|---|---|---|---|
| 15.1 | **PM2 服务管理**: wa_reply_bot / competitor_intel_bot 启停/日志/自启 | PM2 | Systemd / Docker | 不得误停服务 |
| 15.2 | **素材流程**: split_data → final_v14 → watcher → sync_dedup_cache | 流程编排 | 可视化流水线 | 步骤顺序不得变更 |
| 15.3 | **二创流程**: download_to_pending → upload_recreated | 流程编排 | 自动化触发 | 不得跳过下载直接上传 |
| 15.4 | **开发流程**: generate_outreach_list → whatsapp_auto → wechat_auto → calibrate_wa | 流程编排 | 一键执行 | 依赖顺序执行 |
| 15.5 | **合同流程**: generate_pi_pl | 单步执行 | 批量生成 | 不得重复生成 |
| 15.6 | **归档流程**: sync_history_to_feishu | 定时执行 | 事件触发 | 不得遗漏厂家 |

---

## 16. 【已废弃】Customer Social Media Support

**状态**: `[已废弃-逻辑并入模块17，仅存档参考]`
**废弃原因**: 原设计基于"客户抱怨没流量/不会拍视频"的被动触发场景，与实际业务模式不符——模块17本身已是`[Active]`战略级、默认每单执行，不存在被动触发的中间态。原16.1-16.6子步骤能力已被模块17（客户交付通道）与模块19（自有矩阵通道）完整覆盖，不再单独维护。

<details>
<summary>原内容存档（不再生效，仅供追溯）</summary>

**触发条件（已废弃）**：客户主动抱怨"没流量/不会拍视频" OR Melody 判断客户品牌弱

| ID | 原子子步骤 | 🔍可被替代 (来自102清单) |
|---|---|---|
| 16.1 | 客户需求识别（聊天中捕捉） | wa_reply_bot.js 3.5 |
| 16.2 | 素材提取（客户产品/工厂图） | final_v14.py / 百度 Unlimited OCR |
| 16.3 | 视频/图文生成 | MoneyPrinter Turbo / Pixelle-Video / EvoLinkAI |
| 16.4 | 品牌包装（Logo/色系/去水印） | Rephael AI / 二创提示词 R.1-R.7 |
| 16.5 | 多平台适配 | AiToEarn |
| 16.6 | 交付客户 | Google Drive API / WhatsApp 发送 |

</details>

---

## 17. Order+Video 增值服务（资产货币化 · 战略级 · 客户交付通道）

**状态**: `[Active]` 战略级业务模块
**核心资产**: 带 Logo 成品图/视频、包装、出柜视频、工厂通用素材
**防火墙规则**:
- [Red Line] 严禁将带客户 Logo 内容发至 Melody 社媒
- [Red Line] 发给客户的内容必须保留 Logo，符合客户品牌调性
- [Red Line] 不得使用客户素材做非授权用途

### 双通道严格分离

| 通道 | 内容 | 是否去 Logo | 目的 | 归属模块 |
|---|---|---|---|---|
| **Melody 社媒（矩阵）** | 展示工厂实力/工艺细节 | ✅ 必须去 Logo | 拉新、招商、接单 | 去Logo后 → 模块19 |
| **客户社媒** | 他的品牌故事/采购成果 | ❌ 保留 Logo | 帮他卖货、促复购 | 模块17内完成 |

### 订单衍生素材双路径

```
客户订单产出素材（原生素材，自己拍摄，自己拥有素材权）
  ├─ 保留Logo版 → 17.3剪辑(加配音+字幕+剪辑手法，跳过17.3.5去重步骤) → 17.6交付客户(客户自己发)
  │    ⚠️ 不得把未经剪辑的原始素材直接给客户
  └─ 去Logo版   → 17.2去标识化 → 进入19.1素材库 → 19.2批量混剪 → 自己矩阵账号
```

### 原子子步骤（17.1 / 17.3 已细化，17.2/17.4/17.5/17.6 沿用原版）

#### 17.1 素材归集（细化）

| ID | 原子子步骤 | 🔍候选实现 | ⚠️红线 |
|---|---|---|---|
| 17.1.1 | **素材切片**：按场景/语义自动将长视频/无声素材拆分为1-2秒独立镜头 | autocut.video（T148，待验证） | 拆分颗粒度需人工可调，不得暴力切割关键动作 |
| 17.1.2 | **素材自动归档**：AI判断独立镜头归入哪个分镜/章节文件夹，人工批量确认 | autocut.video（T148，待验证） | **归档结果必须人工确认后方可采用，不得跳过确认** |
| 17.1.3 | **三级文件夹规范**：一级=品名，二级=分镜/章节+配音+BGM(大写)，三级=子章节 | — | **同一一级文件夹内素材命名严禁重复，否则工具无法调取** |

#### 17.2 去标识化处理（沿用原版）

| ID | 原子子步骤 | 🔍可被替代 (来自102清单) | 📈可被升级 | ⚠️红线 |
|---|---|---|---|---|
| 17.2 | **去标识化处理**: 人工 PS/剪裁 → 发我社媒时去 Logo | OpenCV 自动模糊/遮盖 | AI 自动擦除 (Inpainting) / SuperVideoEraser | **发我社媒必须去 Logo**；处理完成后方可进入19.1素材库 |

#### 17.3 视频合成与包装（细化）

| ID | 原子子步骤 | 🔍候选实现 | ⚠️红线 |
|---|---|---|---|
| 17.3.1 | **文案提取**：输入参照视频链接，提取口播文案 | autocut.video（T148，待验证） | 仅供结构参考，禁止照搬他人产品卖点措辞 |
| 17.3.2 | **文案裂变**：基于提取文案自动裂变3-10个改写版本 | autocut.video（T148，待验证） | 发我社媒版本需符合"去Logo+工厂实力叙事"，非"产品卖点叙事" |
| 17.3.3 | **提取章节结构**：自动拆分文案为一级分镜/章节列表(通常15-22个) | autocut.video（T148，待验证） | — |
| 17.3.4 | **章节模式填充**：按"根据配音/按视频数/按时长"三种模式自动填充素材 | autocut.video（T148，待验证） | — |
| 17.3.5 | **去重与反检测**：5档重复率+镜像翻转(默认50%,t=前缀豁免)+随机裁剪(100-120%) | autocut.video（T148，待验证） | 需实测抖音原创审核实际通过率，不采信软件自称；**客户交付版（保留Logo，单条交付）可跳过本步，无需防重复检测** |
| 17.3.6 | **音画同步**：诸葛亮排版(时长随素材)/孙悟空排版(时长随配音)两种模式 | autocut.video（T148，待验证） | 整段配音成片≤5条，需逐章节配音才能规模化产出 |

#### 17.4-17.6（沿用原版）

| ID | 原子子步骤 | 🔍可被替代 (来自102清单) | 📈可被升级 | ⚠️红线 |
|---|---|---|---|---|
| 17.4 | **文案生成**: Claude (Melody 人设) → 客户视角夸他 | Humanizer-zh (去AI味) / dbskill (选题/标题) | 多语种本地化 (针对客户国家) | 严禁暴露供应链成本细节 |
| 17.5 | **多格式输出**: 横屏(官网/投标/PPT) + 竖屏(短视频) | 自动裁剪工具 | 自适应分辨率 | 必须适配客户发布渠道 |
| 17.6 | **交付客户**: 网盘链接/WhatsApp 发送 | Google Drive API / AiToEarn (分发) | 自动同步至客户飞书/邮箱 | **严禁发送给非授权联系人**；**严禁直接发送未经17.3剪辑的原始素材** |

### 商业模式

```
客户下单 → 交货
         ↓
  "张总，这批货除了正常交付，我还顺便给您剪了一套完整的品牌宣传视频，您直接拿去发就行。"
         ↓
  ┌─────────────────────────────────┐
  │  客户视角：                        │
  │  ① 不用找摄影团队拍货            │
  │  ② 不用写文案                    │
  │  ③ 拿到手就能发 TikTok/Ins/朋友圈/投标 PPT │
  └─────────────────────────────────┘
         ↓
  提高粘性（客户离不开你的内容）
  → 更高复购
  → 更高溢价
  → 未来可独立成"外贸代运营"副业
```

---

## 18. 配置与数据文件（非脚本，但为核心依赖）

| 文件 | 内容 | 依赖脚本 | 重要性 |
|---|---|---|---|
| `wa_coords.json` | 屏幕坐标映射 (发送/关闭/弹窗) | `whatsapp_auto.py`, `calibrate_wa.py` | ⭐⭐⭐⭐⭐ (物理依赖) |
| `dedup_cache.json` | pHash 指纹库 (黑名单+素材库) | `final_v14.py`, `watcher.py`, `sync_dedup_cache.py` | ⭐⭐⭐⭐⭐ (数据依赖) |
| `同行开发列表.html` | 94家供应商数据 (静态) | `whatsapp_auto.py`, `competitor_intel_bot.js` | ⭐⭐⭐⭐ (数据源) |
| `客户开发列表.html` | 动态客户池 (由 6.1 生成) | `whatsapp_auto.py` | ⭐⭐⭐⭐ (数据源) |
| `二创提示词.txt` | AI 生成规则集 (Rule R.1-R.7) | `upload_recreated.py` 9.1 / `8.5.2` | ⭐⭐⭐⭐⭐ (宪法级) |
| `AGENTS.md` | Claude Code 行为规范 | 所有脚本修改入口 | ⭐⭐⭐⭐⭐ (宪法级) |

---

## 19. 矩阵混剪日更（自有素材批量生产 · 引流获客通道）

**状态**: `[待激活]`
**素材来源**: 日常自主拍摄素材 + 17.2去标识化后的订单衍生素材，统一汇入
**与模块17的关系**: 独立通道，不共用交付对象——17交付客户，19发自己矩阵账号

### 19.1 素材归集（含双源分流红线）

| ID | 原子子步骤 | 🔍候选实现 | ⚠️红线 |
|---|---|---|---|
| 19.1.0 | **素材双源分流**：通用素材(无Logo)直接入库；订单衍生素材(带Logo)先经17.2去标识化处理，确认Logo已完全去除后方可入库 | 人工判定 + OpenCV/Inpainting擦除 | **未经17.2确认去标识化的带Logo素材，严禁进入19.1素材库，防止误发客户Logo到自己社媒** |
| 19.1.1 | **素材切片**：按场景/语义自动将长视频/无声素材拆分为1-2秒独立镜头 | autocut.video（T148，待验证） | 拆分颗粒度需人工可调，不得暴力切割关键动作 |
| 19.1.2 | **素材自动归档**：AI判断独立镜头归入哪个分镜/章节文件夹，人工批量确认 | autocut.video（T148，待验证） | **归档结果必须人工确认后方可采用，不得跳过确认** |
| 19.1.3 | **三级文件夹规范**：一级=品名，二级=分镜/章节+配音+BGM(大写)，三级=子章节 | — | **同一一级文件夹内素材命名严禁重复，否则工具无法调取** |

### 19.2 批量去重混剪

| ID | 原子子步骤 | 🔍候选实现 | ⚠️红线 |
|---|---|---|---|
| 19.2.1 | **文案提取**：输入参照视频链接，提取口播文案 | autocut.video（T148，待验证） | 仅供结构参考，禁止照搬他人产品卖点措辞 |
| 19.2.2 | **文案裂变**：基于提取文案自动裂变3-10个改写版本 | autocut.video（T148，待验证） | 需符合"去Logo+工厂实力叙事"，非"产品卖点叙事" |
| 19.2.3 | **提取章节结构**：自动拆分文案为一级分镜/章节列表(通常15-22个) | autocut.video（T148，待验证） | — |
| 19.2.4 | **章节模式填充**：按"根据配音/按视频数/按时长"三种模式自动填充素材 | autocut.video（T148，待验证） | — |
| 19.2.5 | **去重与反检测**：5档重复率+镜像翻转(默认50%,t=前缀豁免)+随机裁剪(100-120%) | autocut.video（T148，待验证） | 需实测抖音原创审核实际通过率，不采信软件自称 |
| 19.2.6 | **音画同步**：诸葛亮排版(时长随素材)/孙悟空排版(时长随配音)两种模式 | autocut.video（T148，待验证） | 整段配音成片≤5条，需逐章节配音才能规模化产出 |

**量产标准常量**（详见全局常量表 MIN_CHAPTERS_1000 / MIN_MATERIALS_1000 / MIN_CHAPTERS_100 / MIN_MATERIALS_100 / VOICEOVER_CLIP_LIMIT）

### 19.3 自动配音+文案+标题生成

| ID | 原子子步骤 | 🔍候选实现 | ⚠️红线 |
|---|---|---|---|
| 19.3.1 | **文案生成**（Melody人设，去Logo版工厂叙事） | Claude | 不得暴露供应链成本/客户信息 |
| 19.3.2 | **标题生成**（爆款标题公式） | Claude | — |
| 19.3.3 | **语音合成TTS** | autocut.video内置语音合成（T148，待验证） | — |
| 19.3.4 | **去AI味处理** | Humanizer-zh（待安装，🔴最高优先级） | 所有对外文案默认过此步骤 |

### 19.4 矩阵账号分发（引用模块21，本条只保留矩阵专属红线）

| ID | 原子子步骤 | 🔍现役/候选实现 | ⚠️红线 |
|---|---|---|---|
| 19.4.1 | 调用模块21.3执行7平台分发 | 多帖（现役） | — |
| 19.4.2 | 账号轮换：按矩阵账号池分配内容 | 人工排期 | 不做单一大账号，每号1000-3000粉即可 |
| 19.4.3 | 同素材衍生视频防重发 | 人工核对 | 同批次视频不得同时发同一账号，防止自我判重 |

---

## 20. 批量图片加水印（品牌视觉标准化）

**状态**: `[现役]`（自研，已完成）

| ID | 原子子步骤 | 🔍现役实现 | ⚠️红线 |
|---|---|---|---|
| 20.1 | 扫描目标文件夹+子文件夹 | Google Apps Script（自研） | — |
| 20.2 | 加载品牌Logo，可视化调节透明度/大小/角度/间距 | 同上 | 100%保留Logo原始宽高比，不改变专利图形形状 |
| 20.3 | 一键批量处理，输出`_wm`后缀水印图 | 同上 | 不覆盖原图 |
| 20.4 | 存入Google云盘 | Google Drive API | — |

工具链接：`script.google.com/macros/s/AKfycbwRHd.../exec`

---

## 21. 全渠道分发引擎（承接背景说明第7步，模块19.4引用本模块，不重复定义）

**状态**: `[Active]`，主力人工确认+一键分发，暂不开自动发布

| ID | 原子子步骤 | 🔍现役/备用实现 | ⚠️红线 |
|---|---|---|---|
| 21.1 | 内容筛选：按产品标签勾选二创图片/文案/视频 | 飞书素材库 | — |
| 21.2 | 发布前人工确认一次内容 | 人工 | **不打开自动发布开关，保留人工把关** |
| 21.3 | 一键分发7个海外平台 | 多帖（现役，免费开源插件） | 不支持自动定时，需手动点击触发 |
| 21.4 | 规模变大后无人值守定时发布 | AiToEarn（备用） | 启用需账号授权，先用小号测试2-3周 |
| 21.5 | Ins/FB/Threads不自动联动，三平台各自单独发布 | 人工 | 自动同步会导致视频不同步/互动权重打折 |

**已排除方案**：Postiz本地部署（Docker占用过高）、Social-auto-upload（不支持LinkedIn/Pinterest/Threads/X）

---

## 22. 货款台账对账

**状态**: `[Active]`，**全人工，无工具介入**

| ID | 原子子步骤 | 🔍现役实现 |
|---|---|---|
| 22.1 | 收款后人工录入流水号/日期/客户/PI号/金额至飞书货款台账 | 人工 + 飞书表格 |
| 22.2 | 按客户/PI筛选，导出PDF发客户对账 | 飞书导出功能 |

**筛选盲区标注**：这是唯一完全没有自动化候选的环节，若未来Horizon爬到"财务对账自动化"类工具，可匹配到22.1（例如生成PI时自动同步写入台账，省去人工录入）

---
## 23. 实时通话翻译辅助（客户沟通增强 · 人工辅助工具）

**状态**: `[待激活]`
**触发条件**: 与印度/海外客户发起微信/WhatsApp 实时语音或视频通话时
**核心痛点**: 英语听力吃力，无法实时理解客户口语，文字/语音消息已有解决方案，实时通话是唯一盲区
**已覆盖（无需工具）**:
- 微信文字消息 → 微信内置翻译按钮 ✅
- 微信/WhatsApp 语音消息 → 语音转文字后翻译 ✅
**本模块聚焦**: 实时通话过程中的同声传译字幕

| ID | 原子子步骤 | 🔍候选实现 | ⚠️红线 |
|---|---|---|---|
| 23.1 | **系统音频捕获**：通话中实时捕获扬声器/系统音频，不依赖通话平台（微信/WhatsApp均兼容） | BlackHole 虚拟声卡（macOS，免费开源） | 不得将通话录音存储至任何云端 |
| 23.2 | **实时 ASR**：将捕获音频实时转为文字，支持英语/印地语 | v2s（本地，Apple Speech框架，零数据上传）/ Qwen3-ASR-Flash（云端流式，精度更高） | 本地方案优先；云端方案不得传含报价/联系方式/商业细节的对话音频 |
| 23.3 | **实时翻译**：英语/印地语 → 中文，延迟 ≤ 2 秒 | v2s 内置 Apple Translation（本地）/ DeepSeek Chat API（云端） | 同 23.2 |
| 23.4 | **字幕浮窗显示**：翻译结果以悬浮字幕叠加在屏幕上，不干扰通话界面 | v2s / Mac Live Subtitle / 讯飞同传 Mac 版 | 字幕窗口不遮挡关键操作区域 |

**设备场景说明**:
- Mac 端通话 → BlackHole + v2s（推荐，本地全链路）
- 手机端通话 → 另开 iPad/Mac 扬声器旁置捕获（降级方案，待验证）

**工具筛选优先级**（Horizon 打分参考）:
- 支持印地语识别 → +必选条件
- 完全本地运行，不上传音频 → +高优
- macOS 系统音频捕获（非仅麦克风）→ +必选条件
- 手机端支持 → +加分项

---

## 24. 知识视频蒸馏（学习效率加速 · 副业知识储备）

**状态**: `[待激活]`
**触发条件**: 有购买的视频课程（电商/跨境/营销/运营方向），内容过长无时间观看
**核心目标**: 把视频课程压缩成可被 Claude 直接调用的结构化知识 Skill，不需要 Melody 本人逐一观看
**当前知识储备方向**:
- 跨境电商从零起步（平台运营/选品/物流/推广），尚未开始实操，处于知识储备阶段
- TikTok Shop / 独立站 / 菲律宾本地 T 恤小店等方向均有课程待消化

| ID | 原子子步骤 | 🔍候选实现 | ⚠️红线 |
|---|---|---|---|
| 24.1 | **视频字幕提取**：将课程视频批量转录为带时间戳的文字（支持中文课程） | Whisper Transcription（macOS App Store）/ MacWhisper | 不得上传至可能二次使用内容的平台；本地转录优先 |
| 24.2 | **内容结构化整理**：将转录文字按章节/知识点整理为结构化 Markdown 笔记 | Claude（直接喂文字输出大纲+要点） | 输出需保留课程章节编号，方便追溯原始内容 |
| 24.3 | **蒸馏成 SKILL.md**：将结构化笔记打包为 Claude Code 可加载的技能文件 | yao-meta-skill（T146，候选）/ 手动按 SKILL.md 格式整理 | 只存知识框架和要点，不存付费课程原始文字（版权合规） |
| 24.4 | **挂载至 Claude Code**：Skill 加载后，跨境电商相关工具筛选/运营方案设计可直接调用该知识库 | Claude Code `~/.claude/skills/` 目录 | Skill 文件须注明"知识来源：购买课程整理，仅供个人使用" |

**知识领域标签**（供 Horizon 工具映射对照）:

| 标签 | 说明 | 关联模块 |
|---|---|---|
| `跨境电商` | TikTok Shop / 独立站 / 亚马逊 / 菲律宾本地店 | 副业三 |
| `选品` | 爆款逻辑、退货率控制、低成本起步品类 | 副业三 |
| `内容带货` | 短视频脚本、直播话术、爆款标题 | 模块19（矩阵混剪） |
| `供应链` | 菲律宾 T 恤本地供应商对接、备货节奏 | 副业三 |
| `矩阵运营` | 多账号分发、粉丝增长节奏 | 模块19、21 |
| `平台规则` | TikTok / 小红书 / 抖音 算法与违规红线 | 模块19、21 |

**Horizon 工具筛选触发条件**（当看到以下类型工具时，映射到本模块）:
- 视频转文字 / 课程笔记生成 / 知识库构建 / AI 学习助手 → `24.1-24.2`
- SKILL.md 打包 / Agent 知识注入 / Prompt 工程化 → `24.3-24.4`
- 电商选品 AI / TikTok 爆款分析 / 跨境运营工具 → 可匹配此模块知识背景后评分
## AI 筛选宪法（最终版 · 9条铁律）

1. **此文档为唯一真理源**。禁止凭空推荐工具。
2. **替换前必须验证**：新工具必须满足所有 `[Red Line]` 约束（如 pHash≤8, 200条上限）。
3. **升级必须量化**：新工具需在成本/速度/准确率上优于现有实现，且不影响稳定性。
4. **禁止破坏命名**：严禁修改 `RXID__{record_id}__` 命名规则。
5. **禁止删除历史**：所有代码变更、工具替换必须可追溯（退役保留，不物理删除）。
6. **视频清单映射**：工具必须对照 `Video_Tool_DB.csv` 进行映射，不得脱离上下文推荐。
7. **Order+Video 资产增值优先**：任何能服务于模块 17 或模块 19 的工具，评分自动 +1.0（**若同一工具同时映射17和19，加成只计一次，不叠加**）。
8. **替换三定律**：
   - Ⅰ（验证前置）：必须经 Melody 人工测试通过
   - Ⅱ（等价或更优）：成本↓精度≥原 / 精度↑成本≤原 / 解锁新能力不增风险
   - Ⅲ（回滚能力）：旧实现降级为 `[退役保留]`，不物理删除
9. **防火墙不可侵犯**：
   - 发我社媒 → 必须去 Logo
   - 发给客户 → 必须保留 Logo
   - 免费 API → 不传敏感客户数据
   - **带Logo素材未经17.2确认去标识化，严禁进入19库（自有矩阵素材库）**

---

> 📌 本文档与 `Melody_AI_Background.md` 及 `Video_Tool_DB.csv` 同步更新
> 来源：三合一视频清单（102 工具）+ 腾讯文档对话 + Claude 对话 + Order+Video 商业模式 + autocut.video多轮实测投喂
> 更新: 2026-07（新增模块23实时通话翻译/模块24知识视频蒸馏）
